// config.js
const config = {
    // PUBLIC_IMAGE_URL: 'https://bookstore-api-service.onrender.com/image/',
    PUBLIC_IMAGE_URL: `${process.env.REACT_APP_BASE_URL}/image/`,
};

export default config;
